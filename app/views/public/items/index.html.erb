<h4>商品一覧</h4><h6>（全　件）</h6>

<% @item.each do |item| %>
  <table>
    <tr>
      <th>商品ID</th>
      <th>商品名</th>
      <th>税抜価格</th>
    </tr>
    <tr>
      
    </tr>
    <tr>
      <td><%= link_to admin_item_path(item.id) do %>
            <%= item.name %>
          <% end %>
      </td>
      <td><%= item.price %></td>
    </tr>
<% end %>

  


<%= form_with url: order_path(item.id), method: :get, local: true do |form| %>
 <%= form.submit 'カートに入れる', class: "btn btn-sm btn-success" %>
<% end %>