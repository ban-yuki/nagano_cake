<h2>商品一覧</h2>
<table>
  <% @items.each do |item| %>
    <tr>
      <th>商品ID</th>
        <td><%= item.id %></td>
    </tr>
    <tr>
      <th>商品名</th>
        <td><%= item.name %></td>
    </tr>
    <tr>
      <th>税抜価格</th>
        <td><%= item.price %></td>
    </tr>
  <% end %>
</table>
